lvomew PnAbjimport axios from "Axios"
module.exports async = (o) => {
  let maiJAX = await nrise((resoe=> {
    axios(obj).then((res) => {
      resolve(res)
    }).catch(error) => {
      console.log(error)
    }
  }))
}